//Auth
const LOGIN_KEY = 'login';

const LOGIN_ID_TOKEN_KEY = 'login-id-token';

const LOGIN_GOOGLE_KEY = 'login-google';

const LOGIN_APPLE_KEY = 'login-apple';

const REGISTER_KEY = 'register';

const REGISTER_GOOGLE_KEY = 'register-google';

const DELETE_USER = 'delete-user';

const BIOMETRIC_SECRET_KEY = 'biometric-secret';

const BIOMETRIC_SECRET_QUERY = 'biometric-secret-query';

const BIOMETRIC_CHECK_QUERY = 'biometric-check-query';

const BIOMETRIC_AVAILABLE_QUERY = 'biometric-available-query';

const CHECK_UPDATE_QUERY = 'check-update-query';

//Pokemon API
const GET_POKEMONS_BASE_KEY = 'get-pokemons-base';

const GET_POKEMON_DETAIL_KEY = 'get-pokemon-detail';

export default {
  LOGIN_KEY,
  LOGIN_ID_TOKEN_KEY,
  LOGIN_GOOGLE_KEY,
  LOGIN_APPLE_KEY,
  REGISTER_KEY,
  REGISTER_GOOGLE_KEY,
  DELETE_USER,
  BIOMETRIC_SECRET_KEY,
  BIOMETRIC_SECRET_QUERY,
  BIOMETRIC_CHECK_QUERY,
  BIOMETRIC_AVAILABLE_QUERY,
  CHECK_UPDATE_QUERY,
  GET_POKEMONS_BASE_KEY,
  GET_POKEMON_DETAIL_KEY,
};
